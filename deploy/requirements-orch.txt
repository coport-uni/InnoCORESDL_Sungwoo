# Orchestrator-only dependencies (spec section 5): the L2 container has no
# hardware access, so none of the driver submodules are installed here.
httpx>=0.27
pyyaml>=6.0
pydantic>=2.7
fastapi>=0.115
uvicorn>=0.30
